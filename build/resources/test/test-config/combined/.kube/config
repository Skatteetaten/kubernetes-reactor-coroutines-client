current-context: dev/localhost:8080/username
users:
    - name: combined # name is the same as user.home system property
      user:
          token: test-token
    - name: username2/blabla:9090
      user:
          token: abc123
    - name: username3/localhost:8080
      user:
          token: def456